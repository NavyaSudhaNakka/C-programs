#include<stdio.h>
void main()
{
	int n,r,sum=0;
	printf("enter number: ");
	scanf("%d",&n);
	int i=n;
	while(n>0)
	{
		r=n%10;
		sum=sum+r;
		n=n/10;
	}
	if(i%sum==0)
		printf("Harshad Number\n");
	else
		printf("Not a Harshad Number\n");
}
